# PaperBrain 重构改进清单

更新时间：2026-06-02

## 目标

把当前仓库从“每日论文筛选 + 主题母页”重构为“高质量论文分析 + Obsidian 原生标签/属性/Bases 归纳”的研究知识库工具。重点不是堆视觉效果，而是让大量论文可以被稳定检索、组合筛选、分组比较、持续增量维护。

## 联网调研结论

### OpenRouter 模型与调用策略

已避开用户当前不可用的 Claude / OpenAI / Gemini 系列，采用 DeepSeek / Qwen / Z.ai GLM / Moonshot Kimi 组合。

参考来源：

- OpenRouter Provider Routing：支持 `provider.order`、`allow_fallbacks`、`require_parameters`、`data_collection` 等路由控制。https://openrouter.ai/docs/features/provider-routing
- OpenRouter Structured Outputs：支持 `response_format` JSON Schema，并建议配合 `require_parameters: true`。https://openrouter.ai/docs/features/structured-outputs
- OpenRouter Reasoning Tokens：支持 `reasoning.effort` / reasoning tokens，适合精筛和深度分析。https://openrouter.ai/docs/guides/best-practices/reasoning-tokens
- OpenRouter API Parameters：确认 `response_format` 等参数属于 OpenRouter Chat Completions 参数体系。https://openrouter.ai/docs/api/reference/parameters
- DeepSeek 模型页：DeepSeek V4 Pro 适合复杂推理、代码、长上下文综合；DeepSeek V4 Flash 适合高速任务。https://openrouter.ai/deepseek
- Qwen 模型页：Qwen3.7 Max 适合 agentic / coding / long-horizon，Qwen3.5 Plus 与 Qwen3.6 Flash 支持长上下文和多模态，Qwen3-VL 系列适合图像/文档理解。https://openrouter.ai/qwen
- Z.ai 模型页：GLM 5.1 / GLM 5 面向长程 agent、代码、复杂任务；GLM 5V Turbo 适合多模态。https://openrouter.ai/z-ai
- MoonshotAI 模型页：Kimi K2.6 / K2.5 适合长上下文、多模态、代码和 agentic 任务。https://openrouter.ai/moonshotai

推荐组合：

| 调用功能 | 主模型 | Fallback 链 | 理由 |
|---|---|---|---|
| Stage-1 粗筛 | `deepseek/deepseek-v4-flash` | `qwen/qwen3.6-flash`, `z-ai/glm-4.7-flash`, `deepseek/deepseek-v3.2` | 需要快、稳、便宜，粗筛只做 recall-oriented triage。 |
| Stage-2 精筛 | `deepseek/deepseek-v4-pro` | `qwen/qwen3.7-max`, `qwen/qwen3-max-thinking`, `z-ai/glm-5.1`, `moonshotai/kimi-k2.6` | 需要严格评分、长上下文、稳定 JSON。 |
| 深度全文分析 | `deepseek/deepseek-v4-pro` | `qwen/qwen3.7-max`, `z-ai/glm-5.1`, `moonshotai/kimi-k2.6`, `qwen/qwen3-max-thinking` | 需要长文本理解、方法拆解、批判分析。 |
| 视觉/图表理解 | `qwen/qwen3-vl-30b-a3b-thinking` | `qwen/qwen3.5-plus-20260420`, `z-ai/glm-5v-turbo`, `moonshotai/kimi-k2.5` | 当前任务要读 PDF 架构图，需非 Gemini/OpenAI/Claude 的 VLM。 |
| RAG 相关笔记选择 | `qwen/qwen3.6-flash` | `deepseek/deepseek-v4-flash`, `z-ai/glm-4.7-flash` | 扫已有笔记列表，需要快和稳定结构输出。 |
| 播客文稿 | `moonshotai/kimi-k2.6` | `qwen/qwen3.7-max`, `deepseek/deepseek-v4-pro`, `z-ai/glm-5.1` | 长文风格化改写，Kimi / Qwen 更适合长文本表达。 |
| 标签归纳/元数据补全 | `qwen/qwen3.6-flash` | `deepseek/deepseek-v4-flash`, `z-ai/glm-4.7-flash` | 标签应快速、可重复、低成本；高风险时再人工审核。 |

配置策略：

- 在 `config.yaml` 中新增 OpenRouter fallback 链，避免某个供应商地区不可用导致整条流水线中断。
- 所有 JSON 输出型调用继续保留本地 `_sanitize_json`，后续可进一步接入 `response_format` + `provider.require_parameters=true`。
- 视觉选图不能再 fallback 到 Gemini/OpenAI，改成 Qwen-VL / GLM-V / Kimi 多模态链。

### Obsidian 论文归纳方式

参考来源：

- Obsidian Tags：官方支持在 `tags` property 中写标签，也支持通过 `/` 创建 nested tags；搜索 `tag:parent` 会匹配子标签。https://obsidian.md/help/Editing%2Band%2Bformatting/Tags
- Obsidian Properties：属性适合存放小型、原子化、机器可读信息；`tags` 是默认属性之一。https://obsidian.md/help/properties
- Obsidian Bases：Bases 数据来自本地 Markdown 文件及其 properties，适合做表格/卡片式论文库。https://obsidian.md/help/bases
- Obsidian Bases Functions：`hasTag()` 支持 nested tags，适合按领域/方法/任务筛选。https://help.obsidian.md/bases/functions

新方案：删除旧 `Research_Themes` 主题母页体系，替换为 `Research_Index`。

核心原则：

- 标签负责“可组合归纳”：例如 `domain/vla`、`method/world_model`、`task/manipulation`、`evidence/real_robot`、`status/unread`。
- Properties 负责“排序/筛选维度”：例如 `score`、`relevance`、`rigor`、`evidence`、`publication_date`、`domains`、`methods`、`tasks`、`paper_type`、`impact_band`。
- Wiki links 负责“知识关系”：仍保留 RAG 与 gardener 生成的 related work/backlink。
- Bases 负责“日常操作台”：按 score、日期、领域、方法、任务、阅读状态筛选。

## 重构清单

### 1. 本地计划文件

- [x] 新建 `改进.md` 记录模型调研、知识库整理方案、代码修改清单。

### 2. 删除旧主题系统

- [x] 删除 `Research_Themes/` 下所有旧主题页。
- [x] 删除 `script/src/theme_manager.py`。
- [x] 删除 `script/themes.yaml`。
- [x] 删除 `script/rebuild_theme_pages.py`、`script/enrich_empty_themes.py`、`script/patch_themes.py`。
- [x] 从 `script/main.py`、`script/config.yaml`、`script/prompts.yaml`、`script/src/config_loader.py` 中清理 ThemeManager / theme_enrichment / load_themes 相关逻辑。

### 3. 新增 Research_Index 归纳体系

- [x] 新增 `script/src/research_indexer.py`：扫描 `Research_Notes`，规范化 tags/properties，生成聚合页面。
- [x] 新增 `script/build_research_index.py`：手动全量重建索引。
- [x] 新增 `Research_Index/Research_Index.md`：论文库总入口，包含标签体系、快速入口、Dataview 查询。
- [x] 新增 `Research_Index/Paper_Library.base`：Obsidian Bases 论文库视图。
- [x] 新增 `Research_Index/Tag_Guide.md`：标签规范说明。
- [x] 可选生成 `Research_Index/Reading_Queue.md`：按 score、未读状态、领域聚合。
- [x] 主流程在写入详细笔记后自动刷新索引。

### 4. 标签与属性规范

- [x] 扩展 `script/tags.yaml`，建立 nested tag taxonomy。
- [x] 给每篇论文 frontmatter 补齐：
  - `tags`：Obsidian nested tags，如 `domain/vla`、`method/diffusion_policy`。
  - `domains`：领域列表。
  - `methods`：方法列表。
  - `tasks`：任务/应用列表。
  - `paper_type`：method / benchmark / dataset / system / survey / analysis。
  - `impact_band`：must_read / high_value / solid / watch / archive。
  - `reading_status`：unread / reading / read / skipped。
  - `year`：由 `publication_date` 派生。

### 5. 模型配置与调用稳定性

- [x] 更新 `script/config.yaml` 中 OpenRouter 模型与 fallback 链。
- [x] 修改 `analyser.py` 的视觉模型 fallback，去除 Gemini/OpenAI。
- [x] 在 OpenRouter extra params 中预留 provider routing / reasoning 配置。
- [x] 保留 Doubao 作为国内可用备选 provider。

### 6. 代码质量顺手修复

- [x] 清理 `gardener.py` 中重复定义，保留更稳的分层匹配逻辑。
- [x] 更新 README / CLAUDE 中旧主题系统说明。
- [x] 运行基础编译检查。
- [x] 用当前仓库全量生成一次 Research_Index，并检查 Obsidian 入口是否清晰。

### 7. 深度分析笔记质量优化

- [x] 联网参考 Keshav 三遍阅读法，将深度笔记改成“核心快照 -> 阅读地图 -> 方法 walkthrough -> 公式理论 -> 实验证据 -> 复现与问题”的递进结构。
- [x] 保留并强化 `Core Snapshot`，固定包含 `Problem Statement`、`Core Contribution`、`Innovation Origin & Rationale` 三个小节。
- [x] 重写 `script/prompts.yaml` 的深度分析提示词，要求英文输出、完整段落、多角度解释，避免一个大点挤成一个巨大自然段。
- [x] 增强 `Core Theory And Formulas` 要求，明确使用 Obsidian 兼容的行内公式 `$...$` 与行间公式 `$$...$$`，并解释符号、直觉与优化含义。
- [x] 在提示词中加入 Obsidian 兼容的 `**bold**`、`*italic*`、`==highlight==`、callout、Mermaid 约束。
- [x] 在 `script/src/analyser.py` 中新增 refinement pass：先生成初稿，再由强模型系统性改写可读性、公式解释、段落分割与事实谨慎性。
- [x] 将第二轮知识库连接改成只读取优化后的文字笔记，不再重复发送 PDF 图像上下文，降低成本并减少注意力漂移。
- [x] 在 `script/config.yaml` 新增 `refinement_pass_enabled`、`refinement_max_chars`、`refinement_paper_excerpt_chars`，让滚动优化可配置。
- [x] 统一摘要 fallback、视觉 fallback 与主流程的输出清理逻辑，减少旧标题、metadata 残留和装饰性乱码。

### 8. Open Questions 与模型可用性验证

- [x] 将 `Research_Index/Open_Questions.md` 从统一模板问题改为每篇高价值论文一条专属问题。
- [x] 在 `script/src/research_indexer.py` 中新增基于 Core Snapshot、limitations、research ideas、领域/方法标签的开放问题生成逻辑。
- [x] 按 world model、VLA、benchmark/dataset、diffusion policy、reinforcement learning 等类型生成不同问题，减少重复句式。
- [x] 重建 `Research_Index`，确认旧模板问题计数为 0，当前保留 25 条高价值论文问题。
- [x] 用 OpenRouter REST API 快速验证当前 key：`deepseek/deepseek-v4-flash` 调用成功，`anthropic/claude-sonnet-4.6` 返回 403，说明 Claude 当前仍受限。

### 9. 筛选评分精细化与深度分析策略

- [x] 联网参考 PRISMA/Cochrane 的分阶段筛选思想与 NeurIPS 评审维度，将流程保持为“标题摘要粗筛 -> PDF 摘录精筛 -> 质量门槛 -> 深度分析选择”。
- [x] 将粗筛与精筛的所有模型评分从整数改为一位小数，满分仍为 10.0。
- [x] 将 `script/prompts.yaml` 和 `script/src/analyser.py` fallback prompt 的评分 schema 改为 `number`，要求 1.0-10.0、一位小数。
- [x] 在 `script/config.yaml` 中新增动态精筛与深度分析阈值：`screening_second_stage_ratio`、`screening_second_stage_max_k`、`deep_analysis_lower_threshold`、`deep_analysis_extra_threshold`、`deep_analysis_quality_gate`。
- [x] 在 `script/main.py` 中把 Stage-2 精筛数量改为动态 Top-K：至少 10 篇、约当日论文 25%、最多 20 篇。
- [x] 将每日深度分析策略改为：默认选最高分 1-2 篇；低于下阈值不生成；第三名及之后只有达到上阈值才追加生成。
- [x] 全量重建 Research_Index，并将现有 Research_Notes 的 `score` frontmatter 规范为一位小数格式。

### 10. 评分科学性校准与本地检验

- [x] 新增 `script/src/scoring.py`，把模型输出的维度分与仓库内确定性校准逻辑分离：模型负责判断 relevance / novelty / rigor / evidence / reproducibility / confidence / red_flags，代码负责加权、惩罚、封顶和最终一位小数评分。
- [x] 将精筛总分改为可解释校准：默认权重为 relevance 0.30、novelty 0.23、rigor 0.22、evidence 0.15、reproducibility 0.10；低置信度会轻度扣分，red_flags 每条扣 0.35。
- [x] 增加保守封顶规则：低相关不能靠新颖性刷高分；rigor/evidence 任一过低不能进入高分段；confidence 过低、red_flags 过多、reproducibility 极弱都会限制最终分数。
- [x] 将 Stage-2 动态精筛 Top-K 改为 `ceil(total * ratio)`，在 10-20 篇之间动态控制，避免边界论文被四舍五入漏掉。
- [x] 将每日深度分析选择改为纯函数：低于 `deep_analysis_lower_threshold=7.5` 不生成；默认选最高质量 1-2 篇；第三名及之后必须达到 `deep_analysis_extra_threshold=8.8` 才追加。
- [x] 在 `deep_analysis_quality_gate` 中加入 `red_flags_max: 2`，避免高分但风险点过多的论文自动进入深度分析。
- [x] 新增 `script/tests/test_scoring.py`，覆盖一位小数、高质量保留、低相关封顶、弱证据/弱严谨封顶、低置信/红旗惩罚、Stage-2 Top-K、质量门槛、深度分析上下阈值。
- [x] 本地验证通过：`py_compile`、`unittest` 9 项测试、配置读取、`Research_Index` 重建 77 篇笔记均成功。

## 后续建议

- 给筛选与深度分析增加小型回归测试：mock LLM JSON，验证解析和 fallback 不崩。
- 把 `.env.example` 补上，避免 README 提到但仓库中不存在。
- 给 `download_pdf` 的路径写入改成更统一的 `ObsidianWriter.get_pdf_path_from_paper()`，减少短标题/长标题不一致。
- 给所有 LLM JSON 调用逐步加入 schema + `require_parameters`，降低解析失败概率。
- 建议后续增加“人工复审标签队列”：模型自动打标签后，把低置信度标签列入 `Research_Index/Needs_Review.md`。

## 开发工具链路线 A 计划清单

目标：把当前脚本集合收束成稳定、可测试、可诊断、可被 Obsidian 轻量插件调用的本地研究工具链。路线 A 的边界很明确：核心抓取、筛选、深度分析、索引构建继续由本地 Python 负责；Obsidian 插件只做控制台、状态展示、参数输入与结果入口。暂不做路线 B，即不把整条 pipeline 重写成完整 TypeScript 插件。

### 设计原则

- [x] Python 仍是 source of truth：所有论文处理逻辑、配置读取、run state、缓存、PDF 下载、LLM 调用都保留在 Python 层。
- [x] 插件只调用稳定 CLI：插件通过 `child_process.spawn` 调用本地命令，读取结构化 stdout / run-state JSON，不直接侵入业务模块。
- [x] 先收束命令，再做 UI：只有当 CLI 的参数、退出码、日志、状态文件稳定后，才进入插件 MVP。
- [x] 所有阶段都保留命令行可用性：即使不开 Obsidian，也可以完整运行、测试、排障。
- [x] 每个阶段都有验收门槛：新增命令必须有帮助信息、错误提示、最小测试和文档示例。

### Phase 1：命令收束

- [x] 盘点现有入口：`main.py`、`build_research_index.py`、修复脚本、测试脚本、临时验证脚本，标记保留、合并、废弃三类。
- [x] 设计统一命令族：建议收束为 `paperbrain run`、`paperbrain fetch`、`paperbrain screen`、`paperbrain deep`、`paperbrain index`、`paperbrain doctor`、`paperbrain check`。
- [x] 给主流程补齐子命令参数：`--date`、`--provider`、`--arxiv-url`、`--no-podcast`、`--stop-after`、`--resume/--no-resume`、`--force`。
- [x] 保留旧脚本兼容层：旧入口只做 thin wrapper，并在输出中提示迁移到新命令。
- [x] 标准化退出码：成功为 `0`；配置错误、网络不可用、LLM 调用失败、PDF 不可用、写入失败使用不同非零码。
- [x] 统一 stdout / stderr：人类日志走 stderr；给插件读取的机器结果走 stdout JSON。

验收标准：同一个任务只需要记一个命令入口；README 的日常使用表格可以全部改成 `paperbrain ...` 形式。

### Phase 2：路径、配置与输出正则化

- [x] 统一路径基准：所有相对路径都从仓库根或 vault 根解析，禁止隐式依赖当前工作目录。
- [x] 建立路径解析模块：集中处理 `config.yaml`、prompts、cache、PDFs、Research_Notes、Research_Index、run-state 文件路径。
- [x] 规范 paper identity：继续以 `paper_id` / `arxiv_id` 为主键，保证 CLI、run state、Obsidian note、PDF 文件名可互相追溯。
- [x] 规范运行产物：每次运行输出一个固定结构的 run directory，包含 `state.json`、`screening_report.md`、日志摘要、错误摘要和 artifact 路径。
- [x] 规范日志字段：stage、paper_id、title、provider、model、elapsed_ms、retry_count、artifact_path。
- [x] 规范错误对象：错误码、短消息、可执行建议、原始异常摘要、是否可重试。

本地验证通过：在 `wd` 环境中完成 `py_compile`、CLI help、`paperbrain doctor`、`paperbrain check --skip-tests`、局部单测 18 项，以及全量 `paperbrain check` 40 项测试。

验收标准：任意阶段失败后，插件和命令行都能通过同一个 run-state 文件知道失败在哪篇论文、哪个阶段、下一步如何重试。

### Phase 3：工程工具标准化

- [x] 引入 `pyproject.toml`：声明包名、Python 版本、依赖、命令入口、测试配置和格式化配置。
- [x] 选择轻量工具链：建议 `ruff` 负责 lint + format，`pytest` 或保留 `unittest` 但统一入口，后续再逐步补类型检查。
- [x] 新增统一检查命令：`paperbrain check` 串联配置读取、静态检查、单元测试、关键模块 import、路径可写性检查。
- [x] 增加 pre-commit：至少包含格式化、lint、尾随空格、YAML 基础校验。
- [x] 建立测试分层：纯函数单测、网络 mock 测试、文件系统临时目录测试、少量端到端 smoke test。
- [x] 固化 CI 预案：先写本地可跑的 `check`，后续再接 GitHub Actions 或本地计划任务。

本地验证通过：`pyproject.toml` editable dry-run 可解析，`.pre-commit-config.yaml` 与 `.github/workflows/paperbrain-check.yml` YAML 可解析，`paperbrain check --skip-tests --skip-lint` 通过，全量 `paperbrain check` 43 项测试通过。

验收标准：修改任一核心模块后，可以用一个命令判断是否破坏基础行为。

### Phase 4：Doctor 与诊断能力

- [x] `paperbrain doctor config`：检查 YAML、必填字段、模型配置、阈值、Obsidian 路径。
- [x] `paperbrain doctor env`：检查 API key、Python 版本、依赖版本、可写目录、网络代理环境变量。
- [x] `paperbrain doctor arxiv`：检查 arXiv API endpoint fallback、单篇 abs fallback、PDF 下载、缓存冷却状态。
- [x] `paperbrain doctor llm`：用低成本 prompt 验证当前 provider、主模型和 fallback 链。
- [x] `paperbrain doctor obsidian`：检查 vault、笔记目录、PDF 目录、Research_Index 是否存在并可写。
- [x] 输出诊断报告：命令行显示简明结果，同时写入 `Cache/diagnostics/latest.json` 供插件展示。

本地验证通过：`paperbrain doctor config/env/arxiv/llm/obsidian` 与默认 `paperbrain doctor` 均可输出结构化 JSON 并写入 `Cache/diagnostics/latest.json`；live arXiv / LLM 探针保持显式 `--live`，避免默认检查触发网络或模型费用。

验收标准：用户在插件里点一次“诊断”，就能看到具体是配置、网络、模型、PDF 还是路径问题。

### Phase 5：插件桥接层

- [ ] 定义插件调用协议：插件传入 JSON 参数，Python 返回 JSON event stream 或最终 JSON summary。
- [ ] 统一事件类型：`stage_started`、`paper_started`、`paper_updated`、`artifact_written`、`warning`、`error`、`completed`。
- [ ] 为长任务提供取消机制：插件记录进程 pid，点击停止时发送终止信号，Python 负责保存当前 run state。
- [ ] 提供 dry-run：插件可以先预览将执行的命令、配置和输出目录，不立刻调用 LLM。
- [ ] 处理并发锁：同一 vault 同一时间只允许一个主 pipeline 运行，索引构建可排队。

验收标准：插件不需要理解内部业务，只需要消费事件和 run-state，就能可靠显示进度。

### Phase 6：Obsidian 插件 MVP

- [ ] 插件设置页：Python 路径、仓库路径、默认 provider、默认运行时间、vault 路径、是否生成播客。
- [ ] 主控制面板：日期选择、单篇 arXiv 输入框、provider 选择、运行模式、开始/停止按钮、诊断按钮。
- [ ] 进度时间线：Fetch、Coarse、Screen、Deep、Digest、Index、Podcast 七个阶段，显示当前阶段、耗时和错误。
- [ ] 论文队列视图：每篇论文显示 title、score、stage、PDF 状态、note 状态、可重试动作。
- [ ] Artifact 快捷入口：打开详细笔记、打开 PDF、打开每日 digest、打开 screening report。
- [ ] 日志抽屉：默认只显示 warning/error；需要时展开完整日志，支持复制诊断摘要。

验收标准：用户可以不打开终端，在 Obsidian 内完成一次单篇论文分析和一次每日 pipeline 运行。

### Phase 7：插件体验增强

- [ ] Run history：按日期列出历史运行，显示成功、部分成功、失败、耗时和生成物。
- [ ] Retry panel：对失败论文提供“重试 PDF”“重试精筛”“重试深度分析”“重建索引”。
- [ ] Queue presets：保存常用 preset，如“只抓取”“只重建索引”“单篇深度分析”“无播客快速运行”。
- [ ] 可视化质量分：用小型条形图展示 relevance、novelty、rigor、evidence、confidence。
- [ ] 人工复审入口：低置信标签、红旗过多、高分但 PDF 缺失的论文进入 review queue。
- [ ] 轻量通知：运行完成、失败、需要 API key、需要人工处理时用 Obsidian Notice 提醒。

验收标准：插件从“启动器”升级为“日常研究操作台”，但核心业务仍不迁移出 Python。

### UI 概念：PaperBrain Console

界面定位：安静、清晰、研究工具感，不做营销页，不做大面积装饰。第一屏就是可操作控制台。

布局：

- 顶部工具栏：左侧是 `PaperBrain Console` 与当前 vault 名称；右侧是 provider、Doctor、Settings。
- 左栏运行参数：日期、单篇 arXiv URL、运行 preset、podcast 开关、stop-after 分段运行。
- 中央主区：阶段时间线在上，论文队列在下；每篇论文一行，强调 title、score、PDF、note、当前动作。
- 右栏运行详情：当前论文、日志摘要、错误建议、生成物链接。
- 底部状态条：Python 环境、当前命令、run id、耗时、缓存状态。

交互细节：

- 主按钮使用图标 + 短文本：Run、Stop、Retry、Open Note、Open PDF、Rebuild Index。
- 阶段时间线使用紧凑 stepper：完成、运行中、跳过、失败四种状态。
- 论文队列默认高密度表格；点击一行后在右栏展开详情，避免卡片堆叠占空间。
- 错误提示使用可执行文案：例如“PDF 未下载，已跳过深度分析，可稍后重试 PDF”。
- 所有长任务都显示“已保存 run state”，让用户知道中断后可以恢复。

### 联网选色结论

联网参考：

- Radix Colors Scales：提供多组 12-step light/dark 色阶。https://www.radix-ui.com/colors/docs/palette-composition/scales
- Radix Colors Palette Composition：建议按 brand/accent、gray、semantic scales 组合，并说明语义色可用 error / success / warning / info。https://www.radix-ui.com/colors/docs/palette-composition/composing-a-palette
- Radix Colors Usage：色阶可以直接以 CSS variables 使用，并支持 light / dark theme。https://www.radix-ui.com/colors/docs/overview/usage
- Radix Themes Color：accent color 负责主按钮、链接和交互元素，底层由 Radix Colors 驱动。https://www.radix-ui.com/themes/docs/theme/color

选择：采用 Radix Colors 的 `Slate + Sky + Grass + Amber + Tomato`。这套色阶适合工具台：`Slate` 保持长时间阅读和表格扫描的中性底色，`Sky` 做清爽的主操作色，`Grass` / `Amber` / `Tomato` 分别表达成功、警告和错误，整体不会变成单一蓝紫或深色大块。

语义映射：

| 语义 | Radix 色阶 | 用途 |
|---|---|---|
| Neutral | `slate` / `slateDark` | 背景、面板、边框、正文、日志区域 |
| Primary | `sky` / `skyDark` | Run、链接、当前阶段、高亮 focus ring |
| Success | `grass` / `grassDark` | 完成、PDF 已缓存、笔记已写入 |
| Warning | `amber` / `amberDark` | API cooldown、PDF 暂不可用、低置信标签 |
| Danger | `tomato` / `tomatoDark` | 运行失败、配置错误、LLM 调用失败 |

建议 token：

| Token | Light | Dark | 用途 |
|---|---|---|---|
| `--pb-bg` | `var(--slate-1)` | `var(--slate-1)` from dark scale | 主背景 |
| `--pb-panel` | `var(--slate-2)` | `var(--slate-2)` from dark scale | 侧栏、表格背景 |
| `--pb-border` | `var(--slate-6)` | `var(--slate-6)` from dark scale | 分割线、输入框 |
| `--pb-text` | `var(--slate-12)` | `var(--slate-12)` from dark scale | 正文 |
| `--pb-muted` | `var(--slate-11)` | `var(--slate-11)` from dark scale | 次要说明、日志时间戳 |
| `--pb-primary` | `var(--sky-9)` | `var(--sky-9)` from dark scale | 主按钮、当前阶段 |
| `--pb-primary-soft` | `var(--sky-3)` | `var(--sky-3)` from dark scale | 选中行、轻提示背景 |
| `--pb-success` | `var(--grass-9)` | `var(--grass-9)` from dark scale | 成功状态 |
| `--pb-warning` | `var(--amber-9)` | `var(--amber-9)` from dark scale | 警告状态 |
| `--pb-danger` | `var(--tomato-9)` | `var(--tomato-9)` from dark scale | 错误状态 |

视觉规则：

- 背景层级控制在 `slate-1` 到 `slate-3`，边框控制在 `slate-5` 到 `slate-7`，避免 Obsidian 内部显得过重。
- 主操作只使用 `sky-9`，hover 使用 `sky-10`，soft selected 使用 `sky-3`，不要用渐变。
- 状态色只用于徽标、左侧细条、图标和小面积提示，不铺满整块面板。
- 深色模式直接切换到 Radix dark scale，不手写另一套临时颜色。
- 插件 CSS 中优先用语义 token，不在组件里散落具体色阶名。
